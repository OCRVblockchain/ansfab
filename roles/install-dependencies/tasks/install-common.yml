---
- name: Install common packages
  apt:
    name: "{{ item }}"
    state: latest
    update_cache: yes
  loop:
    - npm
    - python3-pip

- pip:
    name: "{{ item }}"
    state: latest
  loop:
    - docker
    - docker-compose